<?php
phpinfo();

/*
$pdo = new PDO('mysql:dbname=omicron;host=mysql', 'dopiet', 'tomek', [PDO::ATTR_ERRMODE => PDO::ERRMODE_EXCEPTION]);

// $query = $pdo->query('SHOW VARIABLES like "version"');

$query = $pdo->query('SHOW DATABASES;');

$row = $query->fetch();

// echo 'MySQL version:' . $row['Value'];

var_dump($row);
*/